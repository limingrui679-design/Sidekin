# Main-branch media is intentionally small enough for ordinary Git. The full
# PNG authoring corpus remains recoverable from archive/full-png-corpus-v2.1.
RuntimeAssets/**/*.webp -filter -diff -merge -text
docs/readme/*.jpg -filter -diff -merge -text
docs/readme/*.png -filter -diff -merge -text
